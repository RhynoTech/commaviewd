name: commaviewd-release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to package (defaults to current ref/tag)'
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: commaviewd-release-${{ github.ref }}
  cancel-in-progress: false

env:
  OPENPILOT_REF: a27efe5796e31f77e97efb33762522f2ddc3b0d0

jobs:
  bundle-and-publish:
    name: Build comma4 release bundle + publish assets
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    steps:
      - name: Checkout CommaView
        uses: actions/checkout@v4

      - name: Resolve release tag
        id: vars
        run: |
          set -euo pipefail
          TAG="${{ github.event.inputs.tag }}"
          if [[ -z "$TAG" ]]; then
            TAG="${GITHUB_REF_NAME}"
          fi
          if [[ -z "$TAG" ]]; then
            echo "No tag resolved" >&2
            exit 1
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Install toolchain + deps
        id: armcapnp
        run: |
          set -euxo pipefail
          sudo dpkg --add-architecture arm64

          CODENAME="$(. /etc/os-release && echo "${VERSION_CODENAME}")"
          sudo mv /etc/apt/sources.list.d/ubuntu.sources /etc/apt/sources.list.d/ubuntu.sources.bak 2>/dev/null || true
          cat <<EOF | sudo tee /etc/apt/sources.list >/dev/null
          deb [arch=amd64] http://archive.ubuntu.com/ubuntu ${CODENAME} main restricted universe multiverse
          deb [arch=amd64] http://archive.ubuntu.com/ubuntu ${CODENAME}-updates main restricted universe multiverse
          deb [arch=amd64] http://archive.ubuntu.com/ubuntu ${CODENAME}-backports main restricted universe multiverse
          deb [arch=amd64] http://security.ubuntu.com/ubuntu ${CODENAME}-security main restricted universe multiverse

          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME} main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME}-updates main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME}-backports main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports ${CODENAME}-security main restricted universe multiverse
          EOF
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            clang \
            g++-aarch64-linux-gnu \
            binutils-aarch64-linux-gnu \
            capnproto \
            python3 \
            libzmq3-dev \
            libcapnp-dev

          capnp_pkg=""
          for pkg in libcapnp-0.8.0 libcapnp-0.9.2 libcapnp-1.0.1 libcapnp-1.0.2; do
            if apt-cache show "${pkg}:arm64" >/dev/null 2>&1; then
              capnp_pkg="$pkg"
              break
            fi
          done

          if [[ -z "$capnp_pkg" ]]; then
            capnp_pkg="$(apt-cache search '^libcapnp-[0-9]' | awk '{print $1}' | sort -V | tail -n1 || true)"
          fi

          if [[ -z "$capnp_pkg" ]]; then
            echo "::error::No arm64 libcapnp runtime package found"
            exit 1
          fi

          sudo apt-get install -y --no-install-recommends "${capnp_pkg}:arm64"

          ARM_CAPNP_SO="$(ls -1 /usr/lib/aarch64-linux-gnu/libcapnp-*.so | head -n1 || true)"
          ARM_KJ_SO="$(ls -1 /usr/lib/aarch64-linux-gnu/libkj-*.so | head -n1 || true)"

          if [[ -z "$ARM_CAPNP_SO" || -z "$ARM_KJ_SO" ]]; then
            echo "::error::Failed to detect arm64 capnp/kj shared libraries"
            ls -la /usr/lib/aarch64-linux-gnu | head -n 200
            exit 1
          fi

          echo "arm_capnp_so=$ARM_CAPNP_SO" >> "$GITHUB_OUTPUT"
          echo "arm_kj_so=$ARM_KJ_SO" >> "$GITHUB_OUTPUT"

      - name: Checkout openpilot source
        uses: actions/checkout@v4
        with:
          repository: commaai/openpilot
          ref: ${{ env.OPENPILOT_REF }}
          path: openpilot-src
          submodules: recursive


      - name: Normalize script permissions (public mirror)
        run: |
          set -euxo pipefail
          chmod +x commaviewd/scripts/*.sh
          chmod +x tools/release/comma4-build-bundle.sh
          chmod +x comma4/*.sh

      - name: Build release bundle
        env:
          OP_ROOT: ${{ github.workspace }}/openpilot-src
          ARM_CAPNP_SO: ${{ steps.armcapnp.outputs.arm_capnp_so }}
          ARM_KJ_SO: ${{ steps.armcapnp.outputs.arm_kj_so }}
          RELEASE_TAG: ${{ steps.vars.outputs.tag }}
        run: |
          set -euxo pipefail
          tools/release/comma4-build-bundle.sh "$RELEASE_TAG"

      - name: Upload release assets to GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.vars.outputs.tag }}
          files: |
            release/${{ steps.vars.outputs.tag }}/commaview-comma4-${{ steps.vars.outputs.tag }}.tar.gz
            release/${{ steps.vars.outputs.tag }}/commaview-comma4-${{ steps.vars.outputs.tag }}.tar.gz.sha256
          fail_on_unmatched_files: true
