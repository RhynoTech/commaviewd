diff --git a/cereal/custom.capnp b/cereal/custom.capnp
index 5398626..184db29 100644
--- a/cereal/custom.capnp
+++ b/cereal/custom.capnp
@@ -484,3 +484,15 @@ struct CustomReserved18 @0xc86a3d38d13eb3ef {
 
 struct CustomReserved19 @0xa4f1eb3323f5f582 {
 }
+
+struct CommaViewHudLite @0x8b4459e4ae4f0c31 {
+  speedMps @0 :Float32;
+  speedClusterMps @1 :Float32;
+  cruiseSetSpeed @2 :Float32;
+  standstill @3 :Bool;
+  steeringAngleDeg @4 :Float32;
+  steeringPressed @5 :Bool;
+  engaged @6 :Bool;
+  isMetric @7 :Bool;
+  started @8 :Bool;
+}
diff --git a/cereal/log.capnp b/cereal/log.capnp
index d14e149..b8da6de 100644
--- a/cereal/log.capnp
+++ b/cereal/log.capnp
@@ -2604,6 +2604,7 @@ struct Event {
 
     # UI services
     uiDebug @102 :UIDebug;
+    commaViewHudLite @150 :Custom.CommaViewHudLite;
 
     # driving feedback
     userBookmark @93 :UserBookmark;
diff --git a/cereal/services.py b/cereal/services.py
index a2fd5f7..d8bd9be 100755
--- a/cereal/services.py
+++ b/cereal/services.py
@@ -104,6 +104,7 @@ _services: dict[str, tuple] = {
 
   # debug
   "uiDebug": (True, 0., 1),
+  "commaViewHudLite": (True, 20., 10),
   "testJoystick": (True, 0.),
   "alertDebug": (True, 20., 5),
   "livestreamWideRoadEncodeIdx": (False, 20.),
diff --git a/selfdrive/ui/ui_state.py b/selfdrive/ui/ui_state.py
index f7a5d44..806e125 100644
--- a/selfdrive/ui/ui_state.py
+++ b/selfdrive/ui/ui_state.py
@@ -64,6 +64,7 @@ class UIState(UIStateSP):
     )
 
     self.prime_state = PrimeState()
+    self._commaview_hud_pm = messaging.PubMaster(["commaViewHudLite"])
 
     # UI Status tracking
     self.status: UIStatus = UIStatus.DISENGAGED
@@ -113,6 +114,7 @@ class UIState(UIStateSP):
     self.sm.update(0)
     self._update_state()
     self._update_status()
+    self._publish_commaview_hud_lite()
     if time.monotonic() - self._param_update_time > 5.0:
       self.update_params()
     device.update()
@@ -178,6 +180,25 @@ class UIState(UIStateSP):
 
       self._started_prev = self.started
 
+  def _publish_commaview_hud_lite(self) -> None:
+    # COMMAVIEW HUD-LITE EXPORT
+    msg = messaging.new_message("commaViewHudLite")
+    hud = msg.commaViewHudLite
+    hud.started = bool(self.started)
+    hud.isMetric = bool(self.is_metric)
+    hud.engaged = bool(self.engaged)
+    if self.sm.recv_frame["carState"] >= self.started_frame:
+      car_state = self.sm["carState"]
+      controls_state = self.sm["controlsState"]
+      v_cruise_cluster = car_state.vCruiseCluster
+      hud.speedMps = float(car_state.vEgo)
+      hud.speedClusterMps = float(car_state.vEgoCluster)
+      hud.cruiseSetSpeed = float(controls_state.vCruiseDEPRECATED if v_cruise_cluster == 0.0 else v_cruise_cluster)
+      hud.standstill = bool(car_state.standstill)
+      hud.steeringAngleDeg = float(car_state.steeringAngleDeg)
+      hud.steeringPressed = bool(car_state.steeringPressed)
+    self._commaview_hud_pm.send("commaViewHudLite", msg)
+
   def update_params(self) -> None:
     # For slower operations
     # Update longitudinal control state
